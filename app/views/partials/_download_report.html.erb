<% if (params[:month].present? || params[:search].present?) && data.any? %>
  <div class="my-auto">
    <%= image_tag 'import.png',
                  width: '30',
                  class: 'mr-20px',
                  data: { 'bs-toggle': 'modal', 'bs-target': '#reportModal' } %>
  </div>
<% end %>
